// Exercise 2.11.2

// Step 1: Declare a variable 'temperature' with the value 28.
// Write your code below

var Temperature = 38, ZZ = 25;
// Step 2: Use the ternary operator to set a variable 'weather' to a string,
    // 'Hot' if the temperature is greater than or equal to 30,
    // 'Warm' otherwise.
// Write your code below
var weather ="";
// if ( 30 >= Temperature ) {Limit = false; }else{Limit = true;} 
weather =  30 >= Temperature ? "Warm" : "Hot"  ;
console.log(Limit, Temperature, weather);
// Expected Output: Warm




// In the terminal, copy & paste the code below and run it to see the result in the console.
// node 02_ControlFlow/11_Ternary_Operator/02_Ternary_Operator_with_Numbers.js